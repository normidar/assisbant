import 'package:assibant/src/app/app_shell.dart';

export 'package:assibant/src/app/app_shell.dart' show AppShell;

// AppShell is the top-level widget — imported into normal_main.dart as MainPage.
typedef MainPage = AppShell;
